import React from 'react';
import {Text} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';
const WishlistScreen = (): JSX.Element => {
  return (
    <SafeAreaView>
      <Text>this is wishlist page</Text>
    </SafeAreaView>
  );
};

export default WishlistScreen;
