import React from 'react';
import {View} from 'react-native';

type ProductListProps = {
  title?: string;
  stackAllName?: string;
  //   data:
};

const ProductList = ({}: ProductListProps): JSX.Element => {
  return <View></View>;
};
export default ProductList;
